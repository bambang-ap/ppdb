export * from "./colors";
export * from "./sizes";

export enum Sheets {
  USER = "users",
  TOKEN = "token",
  STUDENTS = "students",
}

export enum PATHS {
  LOGIN = "/login",
  REGISTER = "/register",
  APP = "/app",
  // Add id to SISWA path
  SISWA = "/app/siswa",
}
